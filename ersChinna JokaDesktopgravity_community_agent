warning: in the working copy of 'classification/classifier.py', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/classification/classifier.py b/classification/classifier.py[m
[1mindex e520f76..b5a9edc 100644[m
[1m--- a/classification/classifier.py[m
[1m+++ b/classification/classifier.py[m
[36m@@ -8,7 +8,7 @@[m [mfrom __future__ import annotations[m
 [m
 import json[m
 import logging[m
[31m-from typing import Literal[m
[32m+[m[32mfrom typing import Dict, Literal, Optional[m
 [m
 from pydantic import BaseModel, Field, ValidationError[m
 [m
[36m@@ -35,9 +35,11 @@[m [mclass ClassificationResult(BaseModel):[m
     sensitivity_flag: bool[m
     reasoning: str[m
     confidence: float = Field(ge=0.0, le=1.0)[m
[32m+[m[32m    model_used: Optional[str] = None[m
 [m
 [m
 def _get_client():[m
[32m+[m[32m    """Return an authenticated Groq client, raising clearly if the key is missing."""[m
     from groq import Groq[m
 [m
     if not settings.groq.api_key:[m
[36m@@ -48,8 +50,27 @@[m [mdef _get_client():[m
     return Groq(api_key=settings.groq.api_key)[m
 [m
 [m
[32m+[m[32mdef _force_sensitivity(result: ClassificationResult, model: str) -> ClassificationResult:[m
[32m+[m[32m    """Stamp model_used and force sensitivity_flag=True for escalation categories."""[m
[32m+[m[32m    updates: Dict[str, object] = {"model_used": model}[m
[32m+[m[32m    if result.category in settings.pipeline.escalation_categories and not result.sensitivity_flag:[m
[32m+[m[32m        updates["sensitivity_flag"] = True[m
[32m+[m[32m    return result.model_copy(update=updates)[m
[32m+[m
[32m+[m
 def classify(item: ContentItem) -> ClassificationResult:[m
[31m-    """Classify a single ContentItem, retrying on transient/format errors."""[m
[32m+[m[32m    """Classify a single ContentItem, retrying on transient or format errors."""[m
[32m+[m[32m    if item.is_empty:[m
[32m+[m[32m        logger.warning("Empty text for item %s - defaulting to general_question.", item.external_id)[m
[32m+[m[32m        return ClassificationResult([m
[32m+[m[32m            sentiment="neutral",[m
[32m+[m[32m            category="general_question",[m
[32m+[m[32m            sensitivity_flag=False,[m
[32m+[m[32m            reasoning="Empty content - no classification possible.",[m
[32m+[m[32m            confidence=0.0,[m
[32m+[m[32m            model_used="none",[m
[32m+[m[32m        )[m
[32m+[m
     client = _get_client()[m
     cfg = settings.groq[m
 [m
[36m@@ -58,7 +79,7 @@[m [mdef classify(item: ContentItem) -> ClassificationResult:[m
     )[m
 [m
     models_to_try = [cfg.classification_model, cfg.fallback_model][m
[31m-    last_error: Exception | None = None[m
[32m+[m[32m    last_error: Optional[Exception] = None[m
 [m
     for model in models_to_try:[m
         for attempt in range(cfg.max_retries):[m
[36m@@ -72,36 +93,36 @@[m [mdef classify(item: ContentItem) -> ClassificationResult:[m
                         {"role": "user", "content": user_prompt},[m
                     ],[m
                 )[m
[31m-                raw = response.choices[0].message.content[m
[32m+[m[32m                raw = (response.choices[0].message.content or "").strip()[m
                 data = json.loads(raw)[m
[31m-                result = ClassificationResult(**data)[m
[31m-[m
[31m-                # Hard safety net: never let a mis-classified escalation[m
[31m-                # category slip through with sensitivity_flag=False.[m
[31m-                if result.category in settings.pipeline.escalation_categories:[m
[31m-                    result.sensitivity_flag = True[m
[31m-[m
[31m-                return result[m
[31m-[m
[31m-            except (json.JSONDecodeError, ValidationError) as e:[m
[31m-                last_error = e[m
[32m+[m[32m                classified: ClassificationResult = _force_sensitivity([m
[32m+[m[32m                    ClassificationResult(**data), model[m
[32m+[m[32m                )[m
[32m+[m[32m                logger.debug([m
[32m+[m[32m                    "Classified %s/%s -> category=%s sensitivity=%s confidence=%.2f model=%s",[m
[32m+[m[32m                    item.platform.value, item.external_id,[m
[32m+[m[32m                    classified.category, classified.sensitivity_flag,[m
[32m+[m[32m                    classified.confidence, model,[m
[32m+[m[32m                )[m
[32m+[m[32m                return classified[m
[32m+[m[32m            except (json.JSONDecodeError, ValidationError) as exc:[m
[32m+[m[32m                last_error = exc[m
                 logger.warning([m
                     "Classification parse failure on model=%s attempt=%d: %s",[m
[31m-                    model, attempt + 1, e,[m
[32m+[m[32m                    model, attempt + 1, exc,[m
                 )[m
                 continue[m
[31m-            except Exception as e:  # rate limit / network — try fallback model[m
[31m-                last_error = e[m
[31m-                logger.warning("Classification call failed on model=%s: %s", model, e)[m
[32m+[m[32m            except Exception as exc:[m
[32m+[m[32m                last_error = exc[m
[32m+[m[32m                logger.warning("Classification call failed on model=%s: %s", model, exc)[m
                 break[m
 [m
[31m-    # Fail-safe default: if the LLM never returns valid structured output,[m
[31m-    # escalate rather than silently auto-drafting a reply to unknown content.[m
[31m-    logger.error("All classification attempts failed (%s) — defaulting to escalation.", last_error)[m
[32m+[m[32m    logger.error("All classification attempts failed (%s) - defaulting to escalation.", last_error)[m
     return ClassificationResult([m
         sentiment="neutral",[m
         category="angry_customer",[m
         sensitivity_flag=True,[m
         reasoning="Classification failed; routed to human review as a safe default.",[m
         confidence=0.0,[m
[32m+[m[32m        model_used="fallback",[m
     )[m
